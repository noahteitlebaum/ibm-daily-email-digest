# IBM Daily Email Digest — AWS Lambda dependencies
# Install with: pip install -r requirements-lambda.txt -t ./package

# Core dependencies (same as local)
feedparser==6.0.11        # RSS/Atom parsing (Google News feeds)
requests==2.32.3          # HTTP for LLM client + feed fetching
python-dotenv==1.0.1      # Load secrets from .env (for local testing)
PyYAML==6.0.2             # Config files (keywords.yaml / settings.yaml)
python-pptx==1.0.2        # IBM-branded PowerPoint deck generation
Jinja2==3.1.4             # HTML email templating
python-dateutil==2.9.0    # Robust date parsing for feed entries

# AWS-specific dependencies
boto3==1.34.0             # AWS SDK for S3 state storage
botocore==1.34.0          # AWS core library (boto3 dependency)

# NOTE: pywin32 is EXCLUDED - not needed for Lambda (no Outlook COM)
# Email delivery uses SMTP only in cloud deployment
