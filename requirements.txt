# IBM Daily Email Digest — dependencies (GitHub Actions runtime)
# Install with:  pip install -r requirements.txt

feedparser==6.0.11        # RSS/Atom parsing (Google News feeds)
requests==2.32.3          # HTTP for the ICA LLM client
python-dotenv==1.0.1      # Load secrets from .env (local runs; no-op in Actions)
PyYAML==6.0.2             # Config files (keywords.yaml / settings.yaml)
Jinja2==3.1.4             # HTML email templating
python-dateutil==2.9.0    # Robust date parsing for feed entries
sendgrid>=6.11.0          # SendGrid email delivery (headless send)
